# Q1. Write a R program to find the maximum and the minimum value of a givenvector [10
# Marks]

# Create a vector with some sample values
my_vector <- c(15, 42, 7, 23, 89, 32)

# Find the maximum value
max_value <- max(my_vector)

# Find the minimum value
min_value <- min(my_vector)

# Print the results
cat("The maximum value in the vector is:", max_value, "\n")
cat("The minimum value in the vector is:", min_value, "\n")
